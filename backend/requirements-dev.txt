# Development / training extras (not needed to serve the API).
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt

matplotlib>=3.8,<3.11     # feature-importance & SHAP summary figures
seaborn>=0.13.0           # notebook-era EDA visualisations
